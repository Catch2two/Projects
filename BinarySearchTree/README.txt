Attempt at using Classes to build this BST.
//

randomArray() -- Create a randomized array with length determined by input.

mergeSort() -- Merge Sort given Array.

mergeSort(randomArray()) -- Sort new randomArray("amount of nodes")

BinarySearchTree.prettyPrint(tree.root) -- Visual output of the tree and its branches.




BinarySearchTree Functions:

tree.buildTree()

tree.insert() -- Add a new Node to a Tree.

tree.find() -- Look up a Node.

mergeSort() -- Sort the array.

tree.remove() -- Remove input node.

tree.levelOrder() -- The Levelorder() function takes a tree as input, and it traverses the tree in level order.
 This means that it visits all of the nodes on the same level before moving on to the next level.

tree.
