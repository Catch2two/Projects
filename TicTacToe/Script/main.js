/* eslint-disable no-unused-vars */
/* eslint-disable no-undef */
const GameFlow = (() => {

})();

// Score Board
const GameBoard = (() => {
  const score = [];
})();

// Players
const Controller = (() => {
  const player1 = {
    name: 'Player One',
    marker: 'X',
  };

  const player2 = {
    name: 'Player Two',
    marker: 'O',
  };
})();
